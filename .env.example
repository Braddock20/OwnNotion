NODE_ENV=production
PORT=10000
DATA_FILE=./data/life-os.json
CORS_ORIGIN=*
API_KEY=
TIMEZONE=Africa/Nairobi

# Durable storage. If set, the server uses PostgreSQL (Neon recommended)
# and ignores DATA_FILE. Leave blank for local JSON-file mode.
# Example Neon connection string:
#   postgresql://user:password@ep-xxxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=
